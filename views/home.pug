doctype html
html
    head
        title= title
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body
        h1 Home | HubSpot APIs
        a(href='/update-cobj') Agregar a esta tabla
        table
            thead
                tr
                    th Email
                    th firstname
                    th lastname
                    th city
            tbody
                each item in data
                    tr
                        td= item.properties.email
                        td= item.properties.firstname
                        td= item.properties.lastname
                        td= item.properties.city
                        